name: bloc_modul
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.0
  equatable: ^1.2.6

  network:
    path: ../network
  repository:
    path: ../repository

dev_dependencies:
  flutter_test:
    sdk: flutter